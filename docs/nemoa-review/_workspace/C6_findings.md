# C6 한소희 (38 / 프리랜서 디자이너) — finding 레코드

> 검토 대상: 라이브 `https://nemoa.vercel.app` (2026-09-18, 375px 모바일 뷰포트)
> 범위: 홈 · 냉장고 · 옷장(옷장/의류/코디) · 마이 · 등록 모달 · 요금제(등록 모달 URL 탭 잠금 화면)
> 이번이 C6 첫 참여. 기존 수정목록 P0/P1/P2에 이미 있는 항목(P2-1/3/4/5/6/13/14/16/20/21, P1-14/17/31/32/38, P0-10/29)은 제외하고,
> 디자인 시스템 렌즈로만 보이는 새 문제를 기록함.

---

- persona: C6 (한소희 38 / 프리랜서 디자이너)
  screen: 옷장 > 코디 > "오늘 입을 코디" 캐러셀
  observed: 한 장의 코디 카드에 `💞 자주 입는 조합` 문자열이 **정확히 두 번** 찍힌다 — 좌상단 흰 pill 배지와 하단 검은 그라데이션 오버레이의 제목. 코드에서 확인: `src/lib/outfitMatcher.ts:127` 이 `reasons` 배열에 `'💞 자주 입는 조합'` 을 push 하고, 같은 함수 `:140-141` 이 `label` 에도 똑같은 문자열을 넣는다. `src/components/closet/OutfitCard.tsx:70` 이 `reasons.slice(0,2)` 를, `:84` 가 `outfit.label` 을 각각 렌더하므로 같은 카드에 같은 문장이 위·아래로 두 번 나온다.
  problem: P1-14에서 "라벨 1회"로 정리하라고 했던 문제가 문자열만 '여름'에서 '자주 입는 조합'으로 바뀐 채 그대로다. 카드 한 장에 같은 말이 두 번 있으면 둘 중 하나는 잘못 붙은 것처럼 보이고, 그 아래 실제 아이템명(`무인양품 옥스퍼드 셔츠 · 리바이스 501 데님 …`)은 오히려 흐린 80% 흰색 10px로 눌려 있다. 내가 알고 싶은 건 "무엇을 입는가"인데 화면은 "왜 추천하는가"를 두 번 말한다.
  lens: 라벨·배지 남용 / 한 컴포넌트 안에서 같은 정보가 반복되면 시스템이 없다는 신호
  priority_guess: P2

- persona: C6 (한소희 38 / 프리랜서 디자이너)
  screen: 홈 "오늘 저장된 코디" 카드 · 옷장 > 코디 아이템 칩
  observed: 저장한 코디 "가을 코디"의 네 칩(`무인양품…`, `리바이스…`, `Nike 에…`, `Pandora…`)이 **전부 같은 티셔츠 라인 아이콘**을 달고 있다. 원인 두 군데를 찾음 — ① `src/components/home/SavedOutfitSuggestion.tsx:46` 이 아이템 종류와 무관하게 `<Shirt size={10} />` 를 하드코딩. ② 이모지→라인아이콘 매핑표 `src/components/EmojiIcon.tsx:163-177` 에서 `👕 👖 🧥 👗 👚 🥻 🧢 🧣 🧤 🧦` **10종이 모두 `Shirt` 한 개**로 매핑됨(바지·원피스·아우터·모자·스카프·장갑·양말이 전부 티셔츠 그림). 같은 화면 아래 옷장 수납 카드는 여전히 컬러 이모지 `👗🧥👕👕🧥` 를 쓰고 있어, 한 스크롤 안에서 같은 옷이 두 가지 그림 문법으로 두 번 나온다.
  problem: 이모지를 라인아이콘으로 통일하는 방향은 맞는데, 통일하면서 카테고리 구분이 통째로 사라졌다. 이모지일 때는 최소한 바지와 원피스가 달라 보였는데 지금은 반지도 셔츠, 운동화도 셔츠다. 아이콘이 정보를 하나도 안 나르면 그건 장식이고, 장식이 네 칸 반복되면 화면이 지저분하기만 하다. 내 옷을 그림으로 못 고르니 결국 4글자로 잘린 브랜드명을 읽어야 한다.
  lens: 아이콘 언어 일관성 — "하나로 통일"이 "하나로 뭉갠 것"이 되면 안 됨
  priority_guess: P1

- persona: C6 (한소희 38 / 프리랜서 디자이너)
  screen: 옷장 > 코디 > 코디 카드 콜라주
  observed: 코디 카드 한 장이 네 조각으로 나뉘는데 조각마다 배경색이 다르다 — 셔츠 칸 하늘색, 신발 칸 아이보리, 데님 칸 연보라, 반지 칸 연분홍. 출처는 `src/lib/categoryImages.ts:39-53` 의 `FASHION_TONE` 으로, 13개 패션 카테고리에 13개 서로 다른 색상군(sky / indigo / stone / rose / amber / orange / cyan / fuchsia / slate / zinc / gray / pink / violet)을 배정한다. 그런데 이 13색은 **코드로 작동하지 않는다**: 아우터(stone-100)·안경(slate-100)·선글라스(zinc-100)·시계(gray-100)는 육안으로 같은 회색이고, 신발(amber-50)↔가방(orange-50), 원피스(rose-50)↔주얼리(pink-50)도 구분 불가. 빈 슬롯도 `OutfitCard.tsx:96` 의 `bg-gray-100` 이라 시계 칸과 같은 색이다.
  problem: 색이 13개나 쓰였는데 배울 수 있는 규칙은 0개다. 코디 카드 6장을 가로로 넘기면 무작위 파스텔 조각보가 지나가고, 카드끼리 구분도 안 되고 "이 앱은 무슨 색 체계를 쓰는가"라는 답도 안 나온다. 옷 사진이 없어서 생긴 자리를 색으로 메운 건데, 그 색이 의미가 없으니 비어 있는 것보다 더 시끄럽다. 차라리 네 칸 전부 같은 중립 톤이고 아이템 실루엣만 달랐으면 훨씬 정돈돼 보였을 것.
  lens: 컬러 시스템 — 색은 의미를 나르거나 아예 중립이어야 함. 의미 없는 다색은 노이즈
  priority_guess: P2

- persona: C6 (한소희 38 / 프리랜서 디자이너)
  screen: 전역 — 옷 카드 계절 태그(옷장 > 의류) vs 제철·레시피·검색 화면의 계절 태그
  observed: 같은 "봄/여름/가을/겨울" 네 단어에 **색 정의가 파일 두 곳에 따로 있고 값이 다르다**.
    · `src/lib/iconMap.ts:65-70` `SEASON_COLOR` — 봄 `text-pink-500/bg-pink-100`, 여름 `amber-500/amber-100`, 가을 `orange-600/orange-100`, 겨울 `sky-500/sky-100`
    · `src/components/closet/shared.ts:13-18` `SEASON_TAG_STYLE` — 봄 `pink-500/bg-pink-50`, 여름 `amber-500/amber-50`, 가을 `orange-500/orange-50`, 겨울 `blue-500/blue-50`
    즉 "가을"이 옷장에서는 orange-500, 제철/레시피/검색에서는 orange-600이고, "겨울"은 한쪽이 sky(청록끼) 다른 쪽이 blue(정통 파랑)로 눈에 띄게 다른 색이다. 배경 농도도 50 ↔ 100으로 갈린다. 덧붙여 같은 태그 줄에 있는 매칭 배지는 `MATCH_STYLE`(`shared.ts:20-24`)이 `brand-success/10`·`brand-primary/10` 같은 브랜드 토큰을 쓰는데, 바로 옆 계절 태그는 생 Tailwind 팔레트라 한 줄 안에서 두 개의 색 체계가 부딪힌다. 여름(amber)과 가을(orange)은 애초에 인접 색상이라 12px pill 크기에서 서로 구분도 안 된다.
  problem: 화면을 옮겼을 뿐인데 같은 단어의 색이 바뀌면, 사용자는 "다른 뜻인가?" 하고 멈춘다. 나는 실제로 옷장의 주황 '가을'과 제철의 주황 '가을'을 다른 배지로 읽었다. P2-16(CARD 상수가 4곳에 따로 정의)과 완전히 같은 병(공용 토큰 없이 화면별 로컬 상수)인데, 이번엔 radius가 아니라 색에서 재현된다. 디자이너 입장에선 "토큰이 없는 게 아니라, 토큰을 만들어 놓고 안 쓴다"가 더 나쁘다.
  lens: 디자인 토큰 단일 소스 — 같은 개념 = 같은 값. 화면별 로컬 상수는 시스템이 아님
  priority_guess: P2

- persona: C6 (한소희 38 / 프리랜서 디자이너)
  screen: 홈 > 8칸 바로가기 그리드 · 전역 카운트 배지
  observed: 홈 8타일이 색상군 8개를 각각 쓴다 — `src/components/home/QuickLinks.tsx:69-76`: 냉장고 blue / 옷장 rose / 제철 emerald / 레시피 amber / 쇼핑 sky / 활동 violet / 프로필 orange / 설정 gray. 라이브에서 실측한 아이콘 색도 일치(냉장고 `lab(54.2 13.3 -74.7)`, 쇼핑 `lab(63.3 -18.4 -51.0)`, 활동 `lab(49.9 55.2 -81.9)` — 파랑 계열만 셋). 냉장고(blue)↔쇼핑(sky), 레시피(amber)↔프로필(orange)은 63×63 타일 안 22px 아이콘 크기에서 사실상 같은 색이고, 정작 브랜드 보라는 핵심인 냉장고·옷장이 아니라 부가 기능인 "활동"이 차지한다. 같은 관점에서 카운트 배지도 통일돼 있지 않다 — `QuickLinks.tsx:100` `bg-brand-accent` + `ring-2 ring-white` 오버행, `WardrobeView.tsx:109` `bg-gray-900` 인라인(링 없음), `FridgeView.tsx:76` 은 같은 자리에서 `bg-rose-500`/`bg-gray-900` 를 색만 바꿔 쓰고, 하단탭은 인디고(#4F46E5). 전부 `text-[10px]`.
  problem: 8색 무지개는 "기능이 여덟 개다"만 말하고 "무엇이 중요한가"는 안 말한다. 위계가 색으로 안 잡히니 8칸이 똑같이 시끄럽고, 그 와중에 브랜드 색은 엉뚱한 타일에 가 있다. 배지도 앱을 한 바퀴 도는 동안 네 가지 모양으로 나타나서, 같은 "개수"라는 정보가 화면마다 다른 물건처럼 보인다. 이런 화면을 포트폴리오에 올린다면 나는 "컬러 롤(brand/neutral/semantic) 정의부터 다시"라고 적을 것이다.
  lens: 컬러 역할 정의(brand/neutral/semantic) 부재 + 컴포넌트 변형 난립
  priority_guess: P2

- persona: C6 (한소희 38 / 프리랜서 디자이너)
  screen: 등록 모달(상품 정보 등록) 입력 방식 탭 · 옷장 > 코디 안내 문구
  observed: 아이콘 통일 작업이 **닿지 못하는 자리 두 종류**가 남아 있다.
    ① 등록 모달의 4개 탭 중 "직접입력"과 "텍스트"가 같은 펜 그림이다 — `src/components/TextImportModal.tsx:88,90` 이 각각 `✏️`·`📝` 를 지정하고, 매핑표(`EmojiIcon.tsx`)가 `✏️→Pencil`, `📝→Edit3`로 보내는데 lucide에서 `Edit3 = PenLine`(`node_modules/lucide-react/dist/cjs/lucide-react.js:21689`)이라 12px 렌더에서는 Pencil과 구별되지 않는다. 가장 많이 쓸 두 입력 방식이 같은 마크를 단 셈. 같은 모달 안에서 사진 업로드 영역은 lucide `Camera` 라인아이콘인데 바로 아래 버튼은 컬러 이모지 🎥(영상 카메라)라 한 뼘 안에서 "카메라"가 두 문법·두 물건으로 나온다.
    ② 이모지가 **데이터 문자열 안에** 박혀 있어 아이콘 컴포넌트가 원천적으로 손댈 수 없다 — `outfitMatcher.ts:127-134` 의 추천 이유가 `'💞 자주 입는 조합'`, `'🌸 봄 매칭'`, `'🧥 추울 때'`, `'🌙 오랜만에'` 처럼 이모지+텍스트 한 덩어리 문자열이고, `src/app/closet/page.tsx:535` 의 본문 안내도 `카드에서 "👕 오늘 입었어요"로 기록할 수 있어요` 로 이모지를 따옴표 안에 넣는다. 그런데 실제 그 버튼의 라벨에는 셔츠가 없고 체크 표시가 붙는다(P2-22 참조) — 안내문이 존재하지 않는 버튼을 인용하고 있다.
  problem: ①은 "네 가지 방법이 있다"고 해 놓고 그림으로는 세 가지만 구분되는 상태라, 탭을 누르기 전에 무엇이 다른지 알 수 없다. ②가 더 구조적이다 — 아이콘을 아무리 컴포넌트로 통일해도 이모지가 로직 파일의 문자열 리터럴로 남아 있는 한 화면에는 계속 컬러 이모지가 섞여 나오고, 안드로이드/iOS 렌더 차이도 그대로 따라온다. P2-1이 말한 "탭 데이터 스키마를 `{icon, label}` 로 분리"가 UI 컴포넌트뿐 아니라 `outfitMatcher` 같은 도메인 로직까지 가야 끝난다는 뜻. 그리고 안내문이 실제 버튼과 다른 그림을 인용하는 건, 화면과 문구를 서로 안 보고 만들었다는 게 그대로 드러나는 자리다.
  lens: 아이콘 언어 일관성 — 표현(이모지/아이콘)이 데이터에 섞이면 시스템화가 불가능
  priority_guess: P2

---

## 확인했지만 새로 적지 않은 것 (기존 항목 재확인)

- P1-14 "카드당 라벨 1회" — 미이행. 문자열만 바뀐 채 2회 반복(위 1번 참조). 단, 슬롯 위계(골격 3fr / 부속 2fr)는 `OutfitCard.tsx:45,57` 로 실제 적용됨 — 반지가 상·하의와 같은 비중이던 문제는 해소.
- P0-29 냉장고 칸 배지 — `FridgeView.tsx:76` 이 여전히 색(rose/gray-900)으로만 의미를 가르고 숫자는 `list.length` 로 같음. 미해소.
- P1-27 냉장고 칸 라벨 잘림("도어 위 …", "도어 중 …") — 375px에서 그대로 재현. 미해소.
- P1-31 / P2-3 타이포 하한 — 배지·칩 대부분 `text-[10px]`, 칸 라벨 `text-[11px]`, 홈 타일 라벨 13.5px. 미해소.
- P1-38 옷 식별성 — 미해소. 단 원인 진단은 갱신 필요(사진 부재뿐 아니라 위 2번의 아이콘 수렴이 겹침).
- P2-13 옷장 탭 스트립 잘림 — 375px에서는 4탭이 들어감(폴더블 344px 재확인 필요).
