# C2 이서연 (27 / 마케터 · 1인가구) — 검토 기록

- 검토 대상: 라이브 `https://nemoa.vercel.app` (2026-09-17, 375×812 모바일 에뮬레이션)
- 범위: 홈 · 옷장(옷장/의류/코디/쇼핑 4탭) · 등록 모달 · 마이 요금제 · 설정
- C2 최초 참여. 기존 P0/P1/P2 표에서 내 이름이 이미 붙은 항목(P2-1 이모지, P2-4 색, P2-7 다크모드, P2-8 공유, P2-14 클리핑, P2-21 아이콘, P1-14 코디 카드, P0-9 safe-area)은 중복 서술하지 않고, 이번에 **새로 보인 것** 위주로 적는다.

---

- persona: C2 (이서연 27 / 감성·트렌드, 옷장·코디 메인, SNS 공유 습관)
  screen: 옷장 > 코디 탭 (오늘 입을 코디 캐러셀) + 코디 상세 바텀시트
  observed: 코디 1개("가을 코디")를 상세에서 "코디 저장" 한 번 눌렀더니, 토스트 `"가을 코디" 저장됐어요` 직후 **캐러셀 6장 중 4장의 제목이 `💞 자주 입는 조합`으로 바뀜**. 바뀐 카드에는 내가 저장한 적도, 입은 적도 없는 조합(`무인양품 옥스포드 셔츠 · 룰루레몬 요가 레깅스 · Nike 에어포스 1`)까지 포함. 같은 카드 안에서 배지도 `💞 자주 입는 조합`이라 **제목과 배지가 완전히 같은 문자열 2번**. 반대로 옷장 > 의류 탭의 같은 옷들은 전부 `아직 안 입음`이라고 적혀 있음. (코드상 `outfitMatcher.ts:120-141` — 저장 코디의 아이템 쌍 하나만 겹쳐도 `coBoost>0` → `label`을 '💞 자주 입는 조합'으로 덮어씀. 신발 `에어포스 1`이 6장 전부에 들어가 있어 한 번 저장이 거의 전체로 번짐)
  problem: 앱이 내 기록을 지어낸다. 두 탭을 왔다 갔다 하면 "아직 안 입음"인 옷이 "자주 입는 조합"이 되는 게 바로 보여서, 이 화면이 하는 말 전체(오늘 어울림, 오랜만에)를 못 믿겠다. 코디 기록하려고 쓰는 앱인데 기록이 부풀려지면 쓸 이유가 없다. 게다가 저장 후엔 카드 이름이 계절도 아니고 조합 설명문이라, 저장된 코디 목록도 `가을 코디` / `💞 자주 입는 조합` 처럼 이름이 겹치거나 이모지가 박힌 문장으로 쌓인다(이름 바꾸는 칸 없음 — `OutfitDetailModal.tsx:51` 이 `outfit.label`을 그대로 저장명으로 씀).
  lens: "예뻐야 계속 써요" 이전에, 내 옷 기록을 대신 써주는 앱이 사실이 아닌 말을 하면 끝이다
  priority_guess: P1

- persona: C2 (이서연 27 / 감성·트렌드, 옷장·코디 메인)
  screen: 옷장 > 코디 탭 — 캐러셀 카드 라벨
  observed: 저장 전 상태에서 6장 카드의 제목이 **전부 `가을 코디`**, 배지도 **전부 `🍂 가을 매칭` + `🌙 오랜만에` 2개로 동일**. 다른 건 카드 하단 작은 회색 한 줄(`무인양품 옥스포드 셔츠 · 리바이스 501 데님 · Nike 에어포스 1` → `… · 에잇세컨즈 슬랙스 · …`)뿐인데 그마저 `…`로 잘린다. 하단 안내는 `6개 코디 · 좌우로 넘겨보세요`.
  problem: P1-14에 적힌 "6장이 문자 그대로 동일"은 **조합 자체는 달라졌지만 이름·배지는 아직 100% 동일**한 상태로 남아 있다. 6번 넘겨도 같은 카드를 보는 느낌이라 2장 만에 스와이프를 멈췄다. 코디는 이름으로 기억하는 건데("출근룩", "데이트룩") 전부 "가을 코디"면 저장해도 나중에 못 찾는다.
  lens: 코디를 고르는 재미가 없으면 옷장 탭을 열 이유가 없다
  priority_guess: P1

- persona: C2 (이서연 27 / 감성·트렌드, 옷 표현의 조악함에 민감)
  screen: 옷장 > 의류 탭 (내 옷 목록)
  observed: `룰루레몬 요가 레깅스`와 `리바이스 501 데님`의 썸네일이 **똑같은 파란 청바지 이모지 👖**. `무인양품 옥스포드 셔츠`와 `Maison Kitsuné 스웻셔츠`도 똑같은 👕. 의류 10벌이 사실상 👕/👖/👟/💍 4가지 그림으로만 표현된다. 코디 카드의 2×2 콜라주도 같은 4개 이모지가 파스텔 사각형 위에 놓인 형태.
  problem: 요가 레깅스가 청바지 그림으로 나오는 건 단순히 안 예쁜 게 아니라 **틀린 그림**이다. 목록을 훑을 때 이름을 한 글자씩 읽어야 내 옷을 찾을 수 있어서, 종이에 적는 것보다 나은 점이 없다. 사진 추가 기능(카드의 📷)은 있지만 21벌을 한 장씩 찍어야 비로소 옷장처럼 보이고, 그 전까지는 앱이 "내 옷장"이 아니라 남의 샘플로 보인다. 친구에게 보여줄 생각이 안 든다.
  lens: 옷 앱인데 옷이 안 보인다
  priority_guess: P1

- persona: C2 (이서연 27)
  screen: 옷장 > 코디 > 코디 상세 바텀시트
  observed: 메인 버튼 라벨이 **`✓ ✓ 오늘 입었어요` — 체크 표시가 두 개**. (`OutfitDetailModal.tsx:156-157` lucide `<Check>` 아이콘 + 문자열 `'✓ 오늘 입었어요'` 동시 사용) 같은 시트의 항목 행도 왼쪽 썸네일이 👕인데 바로 오른쪽 설명줄이 `👕 상의 · M · 면`으로 **같은 이모지를 한 줄 안에서 두 번** 씀(4개 항목 전부).
  problem: 화면에서 제일 큰 보라색 버튼에 체크가 두 개 찍혀 있는 걸 보는 순간 "이거 덜 만든 앱이구나" 했다. 스크린샷 찍어 올릴 화면이 아니다.
  lens: 디테일이 무너지면 그 앱은 예쁜 앱이 아니다
  priority_guess: P2

- persona: C2 (이서연 27 / 브랜드 감각)
  screen: 옷장 > 쇼핑 탭 · 내 정보 > 쇼핑 탭 (제휴 카드)
  observed: 제휴처가 브랜드 로고 없이 이모지 한 개로 표시됨 — `무신사 👕` / `29CM ✨` / `W컨셉 🎀` / `에이블리 💄` / `지그재그 ⚡` / `당근마켓 🥕` / `번개장터 ⚡` / `KREAM 👟` / `옷캔 👕` / `굿윌스토어 🤝` / `마켓컬리 🌙`. **지그재그와 번개장터가 같은 ⚡**, **무신사와 옷캔이 같은 👕**. 이름도 `아름다운…`, `SSG·이…`처럼 잘림.
  problem: 무신사·29CM는 내가 매일 쓰는 앱인데, 로고 대신 랜덤 이모지가 붙어 있으니 진짜 제휴인지 그냥 링크만 걸어둔 건지 의심스럽다. 마켓컬리가 초승달 🌙인 건 아무리 봐도 이유를 모르겠다. 같은 ⚡이 두 개 있어서 어디로 가는 버튼인지 이름을 읽어야 알 수 있다. 여기서 옷을 살 생각은 안 든다.
  lens: 브랜드를 아무렇게나 다루는 앱은 내 옷도 아무렇게나 다룰 것 같다
  priority_guess: P2

- persona: C2 (이서연 27)
  screen: 등록 모달 > 직접입력 > 옷 등록 (2단계)
  observed: 옷을 등록하는 흐름인데 이름 칸 placeholder가 **`이름 입력 (예: 계란)`** (`TextImportModal.tsx:677` 하드코딩, 식품/의류 분기 없음). 같은 화면 상단 안내는 `네모아가 추출한 목록입니다`인데 나는 AI를 안 쓰고 직접입력으로 들어왔다(P0-32 미해소 확인). 1단계 탭 스트립의 `URL` 탭에 달린 🔒 배지는 탭 컨테이너 밖으로 넘쳐 모서리에 잘린다.
  problem: "옷 등록" 버튼을 눌러 들어왔는데 예시가 계란이면 내가 잘못 눌렀나 싶어 뒤로 나가게 된다. 옷 앱이 아니라 냉장고 앱에 옷이 얹혀 있다는 인상이 여기서 확정됐다.
  lens: 나는 옷장 쓰러 왔는데 앱은 계속 냉장고 얘기를 한다
  priority_guess: P2

- persona: C2 (이서연 27 / SNS 공유 습관, 다크모드 사용자)
  screen: 전역 — 코디 상세 / 설정
  observed: 코디 상세 시트의 버튼은 `코디 저장`·`오늘 입었어요` 두 개뿐, **공유·이미지 저장 버튼 없음**(코드 `navigator.share`/`canShare` 여전히 0건). 설정 화면 전체를 훑어도 테마·다크모드 항목 없음(`dark:` 0건, `prefers-color-scheme` 0건). 알림 설정에는 `코디 추천 알림` 토글이 있어서 코디를 밀어주긴 한다.
  problem: P2-8·P2-7이 이번에도 그대로다. 잘 나온 코디를 스토리에 올릴 수가 없어서, 결국 화면 캡처해서 잘라 쓰는데 그러면 상단 `🍂 가을 매칭` 배지랑 잘린 글자까지 같이 찍혀 못 쓴다. 밤에 침대에서 여는 앱이 순백색으로 켜지는 것도 매번 눈이 아프다. **코디 추천 알림을 켜두면 앱이 코디를 보여주는데, 그걸 남에게 보여줄 방법은 없다** — 유도와 출구가 안 맞는다.
  lens: 공유할 수 없는 예쁨은 나한테 절반짜리다
  priority_guess: P2

---

## 확인했지만 새로 적지 않은 것 (기존 항목 그대로 재현)
- 하단탭·마이 탭 라벨의 이모지 혼용, `🧊` 의미 중복 → P2-1 / P2-21
- 카드 제목·요금제 비교표 행 라벨(`클라우드 …`, `레시피 컬…`, `프로필(가…`) 잘림 → P2-20
- 계절 칩(봄 분홍 / 여름 주황 / 가을 주황)과 상태 배지 색이 카드마다 제각각 → P2-4
- 옷장 첫 진입 시 `신발/가방/액세서리` 칸이 반투명 상태로 잠깐 멈춰 보임 → P1-16

## 검토 중 부수효과
- 브라우저 도구 좌표가 어긋나 의도치 않게 설정 화면의 `지금 백업`이 눌렸고, `nemoa-backup-2026-09-17.json` 로컬 다운로드가 1회 발생했다. 외부 전송은 없음(로컬 JSON 내보내기).
- 검토를 위해 코디 1건을 저장했다(`localStorage: nemoa-saved-outfits` 에 `"가을 코디"` 1건). 위 첫 번째 발견의 재현 조건이므로 남겨 둔다.
