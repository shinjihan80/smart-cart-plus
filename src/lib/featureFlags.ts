/**
 * 결제(Pro 구독) 기능 killswitch.
 *
 * 사업자등록 전이라 정기결제를 실제로 받으면 안 되는 상태 — 프론트/백엔드
 * 양쪽에서 전부 이 값을 확인해 결제 관련 동작을 막는다.
 *
 * 사업자등록 완료 후 재오픈: 이 값을 true로만 바꾸면 됨(로직은 이미 다 만들어져 있음).
 * 그 전까지 수익은 광고(카카오 애드핏 배너 + 보상형 광고)로만 발생.
 */
export const PAYMENTS_ENABLED = false;
